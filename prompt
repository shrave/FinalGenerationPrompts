USER_PROMPT_TEMPLATE = """Write a self-contained JUnit test for the target method: {source_method} in the provided CLIENT CODE (path: {client_rel_path}). The test must check whether {source_method} is affected by the vulnerability identified as {vulnerable_id} or affected by vulnerable APIs: {vulnerable_api_list}. The vulnerable call path starts at {source_method} and reaches the sink {sink_method}; the full path is provided in CALL PATH.

Construct inputs using the same *semantic payload content* as the provided reference test {test_function_name}, adapted as necessary to match the signature and data types of {source_method} (e.g., ByteBuffer, byte[], files). Do NOT reuse test structure or helpers from the reference test.

TEST FUNCTION (reference — payload content only):
{test_function_block}

CLIENT CODE (reference path):
{client_rel_path}

CALL PATH (source → intermediate hops → sink):
{call_path_block}



Rules for test generation:
- **Do NOT mock or stub any production classes or methods that are part of the specified call path (from {source_method} to {sink_method}).**
- Mocking or stubbing is permitted ONLY for incidental dependencies that are NOT on the call path and are required solely to make the test compile.
- Use the test framework already configured in the repository (prefer JUnit 5 only if already configured); include all required imports.
- The generated test MUST be discoverable and executable by the repository’s existing test runner without manual intervention:
  * If Maven is used, ensure Surefire-compatible test discovery (e.g., class name ends with *Test, correct package under src/test/java).
  * If Gradle is used, ensure Gradle test discovery works with the configured test framework.
- Always include explicit assertions verifying the function’s behavior or exception type/message — even when the test is expected to fail — to ensure every test path has at least one assert statement.
- Use Arrange → Act → Assert structure; add comments describing expected outcomes for vulnerable vs patched implementations.
- Keep the test file self-contained and compilable without modifying any existing production/source files.
- If required to make the test compile or execute successfully, you may apply minimal build-system fixes (pom.xml / build.gradle test configuration), but you must NOT change any dependency/library versions. If build changes are necessary, output them as a unified diff in addition to the full test file.
- Explicitly state the detected library versions relevant to the vulnerability (e.g., affected dependency and version) in comments in the test file.

Test independence requirements:
- Treat all files under src/test/java/** and any previously generated tests as nonexistent.
- Do NOT read, reference, adapt, or pattern-match any existing test files, test utilities, base test classes, or helper methods.
- The generated test must be logically independent and self-contained, even if other tests exercise similar logic.

Test data and payload requirements:
- Construct all required payloads, inputs, or artifacts programmatically within the test.
- Do NOT assume the presence of any external files.
- If the test requires files (e.g., JSON files, ZIP archives, binary payloads), create them at runtime using temporary directories or in-memory buffers.

Test behavior expectations:
1. The new test must use the same *semantic payload content* as the reference test, adapted as needed to match the entrypoint and data types of {source_method}.
2. The new test must **fail** when the vulnerable library/version is present so CI/build turns red.
3. On patched versions, the test must pass by observing the correct blocking behavior (e.g., a security-related exception or prevention of an unsafe side effect).
4. The test outcome must be determined by real library/runtime behavior, not by mocked or simulated outcomes.
5. Avoid brittle exact-match assertions on exception messages unless the message is guaranteed stable; prefer type checks and substring contains checks where appropriate.


Process after initial test generation:
STEP 0: Determine the correct build root and test runner by inspecting repository build files.
- If Maven: locate the nearest pom.xml for the module that contains {client_rel_path} and run commands from that directory.
- If Gradle: locate settings.gradle(.kts) / build.gradle(.kts) for the module that contains {client_rel_path} and run commands from that directory.

STEP 1: Run **only** the new test class and verify results; do not run other tests.
- If Maven is used, run only the new test via Maven Surefire. Use a local Maven repo under the build root to avoid sandbox write issues:
  mvn -q -Dmaven.repo.local=<BUILD_ROOT>/.mvn_local -Dtest=<NewTestClassName> test
- If Gradle is used, run only the new test via Gradle test task. Use a local Gradle user home under the build root:
  GRADLE_USER_HOME=<BUILD_ROOT>/.gradle_local ./gradlew test --tests <package.NewTestClassName>

STEP 2: Provide a concise self-critique to assess whether the test is compilable and triggers the PoV as expected on the current (vulnerable) environment. If the test is uncompilable, not discovered by the runner, or does not produce the expected vulnerable behavior, revise the test (and only minimal build config if required) to fix those issues.

STEP 3: REPEAT STEPS 1-2 up to 3 times. STOP the repetition earlier if the test is compilable, discovered by the runner, and the PoV behavior matches expectations.
- For each repetition, report:
  * status (success/failure),
  * short notes on errors fixed,
  * the selected runner command and why it was selected (Maven/Gradle, module root).
- Do NOT fabricate logs. If execution cannot be performed due to environment restrictions, clearly state what prevented execution and limit claims accordingly.
- **Do not output “next steps” or instructions in place of work. Always output the full, final test source file content for each round as needed.**

"""
